# Typecasting = the process of converting a variable from one data type to another
#               str(), int(), float(), bool()

name = "Test"
age = 14
gpa = 3.2
is_admin = False

age = str(age)
print(age)

name = bool(name)
print(name)
# Without a name, the result will lead to False
